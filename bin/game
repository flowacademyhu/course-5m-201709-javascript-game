#!/usr/bin/env nodejs
'use strict';

const readlineSync = require('readline-sync');
const inputText = require('../lib/input')

while (true) {
    inputText(readlineSync.question('>> '));
    if (quit(inputText())) break;
}

function quit(inputText) {
    return inputText == 'kilépés'
}