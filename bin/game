#!/usr/bin/env nodejs
'use strict';

const inputText = require('../lib/input')
const command = require('../lib/command')

while (true) {
    command(inputText);
    if (command.exit) break;
}

function quit(inputText) {
    return inputText == 'kilépés'
}